// Copy this file to config.js and fill in your Apps Script URL
// config.js is gitignored — never commit the real URL
window.SHEET_URL = '';
// Optional: webhook (e.g. an n8n Webhook node) that receives people who ticked the consent box. Leave empty to disable.
window.MAILING_LIST_URL = '';
